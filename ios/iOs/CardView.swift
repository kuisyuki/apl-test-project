import SwiftUI

struct CardView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("バーコードが表示されるエリア")
                .font(.title2)
            Spacer()
        }
    }
} 