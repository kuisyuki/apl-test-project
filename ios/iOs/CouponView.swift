import SwiftUI

struct CouponView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("お店を選ぶ")
                .font(.title2)
            Spacer()
        }
    }
} 