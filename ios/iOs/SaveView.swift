import SwiftUI

struct SaveView: View {
    var body: some View {
        VStack {
            Spacer()
            Text("ためるサービス")
                .font(.title2)
            Spacer()
        }
    }
} 